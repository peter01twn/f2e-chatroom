import { ResizeDraggerDirective } from './resize-dragger.directive';

describe('ResizeDraggerDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new ResizeDraggerDirective();
  //   expect(directive).toBeTruthy();
  // });
});
